import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'confundo-episko',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
